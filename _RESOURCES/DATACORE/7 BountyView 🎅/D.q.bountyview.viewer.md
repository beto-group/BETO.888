
BROKEN damn
	but idk since works fine in other vault


```datacorejsx
////////////////////////////////////////////////////
///             Viewer Entry Point               ///
////////////////////////////////////////////////////

// Retrieve the View component from the setup file (update the file path if needed)
const { ViewBounty } = await dc.require(dc.headerLink("_RESOURCES/DATACORE/7 BountyView 🎅/D.q.bountyview.component.md", "ViewComponentBounty"));

// Render the View component with any initial settings override (if required)
return <ViewBounty />;

```
